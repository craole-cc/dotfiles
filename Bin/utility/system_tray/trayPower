#!/bin/sh

#@ Declare command information
_APP="cbatticon"
_ARGS="-i notification $(find /sys/class/power_supply | grep BAT)"

# #@ Validate app availability
# weHave --check "$_APP"

# #@ Kill all active instances of the app
# murda "$_APP"

# #@ Launch with or without arguments
# if [ "$_ARGS" ]; then
#   eval "$_APP" "$_ARGS"
# else
#   eval "$_APP"
# fi
