#!/bin/sh

# [ "$Root" ] || exec sudo -- "$0" "$@"
# if [ ! "$Root" ] ;then
#     alias sudo='sudo ' 
#     type sudo
#     sudo "$@"
# fi

# type -a "$@" | grep -o -P "(?<=\`).*(?=')" | xargs sudo
# type -a pacQuery | grep -o -P "(?<=\`).*(?=')" | xargs sudo
sudo "$1" shift "$@"

