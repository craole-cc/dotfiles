#!/bin/sh


# sys_INFO="$(
#   printf "%s-%s | %s@%s | %s" \
#     "$(os.type.fetch)" \
#     "$(os.distro.fetch)" \
#     "$(username.fetch)" \
#     "$(hostname.fetch)" \
#     "$(basename "$(shell.fetch)")"
# )"
